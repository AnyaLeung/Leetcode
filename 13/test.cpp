#include <iostream>

using namespace std;

enum RomToInt{
    I = 1,
    V = 5,
    X = 10,
    L = 50,
    C = 100,
    D = 500,
    M = 1000
};

int main(void){
    int res = 0;
    (1>2)? {res += 1;res +=2;} : {res -=1; res -=2;};
    return 0;
}
